Commandes pour cloner : 
git clone liendegithub

Commandes de push : 
git push - push le commit sur git
git push <remote> <branche> - push le commit en spécifiant un repo et une branche
git push -all <remote> - pull les derniers commits d'une branche

Mettre à jour le dépôt local:
git pull

Mettre à jour le dépôt local d'une branch spécifique:
git pull origin MA_BRANCH

Créer une branche : 
git branch nomdelabranche

Pour vérifier qu'elle a bien été créer on peut utiliser : 
git branch - cela va lister les branches qui sont dans le repo actuel

Basculer / changer de branche : 
git checkout nomdelabranche
